package victor.training.sourcing.reactor;

public class OldUsersAreDeactivatedPolicy {
  // TODO if last user login > 1 yr ago -> inactivate user

  // TODO if user is inactive -> do not allow login + send alarm event
}
