package victor.training.sourcing.reactor;

public class UserShouldNotLogin {
  // TODO if last user login > 1 yr ago -> inactivate user

  // TODO if user is inactive -> do not allow login. if loggedIn -> send alarm event
}
